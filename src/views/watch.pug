extends base.pug

block content 
        //- h2 #{video.views}
        div
                p=video.description
                small=video.createAt

        a(href=`${video.id}/edit`) Edit Video &rarr;
        br
        a(href=`${video.id}/delete`) Delete Video &rarr;
  

